# Data folder

`asean-hub.json` is runtime data and is intentionally ignored by Git.

For a new environment, create it from the sample file:

```bash
cp data/asean-hub.example.json data/asean-hub.json
```

Do not commit production data, account records, sessions, email settings, or backups.
